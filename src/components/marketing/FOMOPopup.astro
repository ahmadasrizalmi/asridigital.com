---
import Icon from '../ui/Icon.astro';
---

<!-- FOMO Toast Container - PRD Section 7.4 -->
<div id="fomo-container" class="fixed bottom-4 left-4 z-50 space-y-3 max-w-sm">
  <!-- Toasts will be injected here by JS -->
</div>

<script>
  // FOMO Social Proof Implementation - PRD Section 7.4
  interface RecentSale {
    firstName: string;
    productName: string;
    timeAgo: string;
  }

  const mockSales: RecentSale[] = [
    { firstName: 'S***', productName: 'Animasi Muslim Kids Studio', timeAgo: '5 menit' },
    { firstName: 'B***', productName: 'Lifetime All-Access Pass', timeAgo: '12 menit' },
    { firstName: 'A***', productName: 'Sahabat Guru PAUD Islami', timeAgo: '1 jam' },
  ];

  const container = document.getElementById('fomo-container');
  let currentToast = 0;

  function createToast(sale: RecentSale): HTMLElement {
    const toast = document.createElement('div');
    toast.className = 'card p-4 flex items-start gap-3 transform transition-all duration-500 translate-x-[-100%] opacity-0';
    toast.innerHTML = `
      <div class="w-10 h-10 rounded-full bg-primary/20 flex items-center justify-center text-sm font-bold text-primary shrink-0">
        ${sale.firstName.charAt(0)}
      </div>
      <div class="flex-1 min-w-0">
        <p class="text-sm text-text-primary">
          <span class="font-semibold">${sale.firstName}</span> baru saja membeli
        </p>
        <p class="text-sm text-primary font-medium truncate">${sale.productName}</p>
        <p class="text-xs text-text-muted mt-1">${sale.timeAgo} yang lalu</p>
      </div>
      <button onclick="this.parentElement.remove()" class="text-text-muted hover:text-text-primary">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M18 6L6 18M6 6l12 12"/>
        </svg>
      </button>
      <div class="absolute bottom-0 left-0 right-0 h-0.5 bg-primary/20">
        <div class="h-full bg-primary toast-progress" style="animation: shrink 4s linear forwards"></div>
      </div>
    `;
    return toast;
  }

  function showToast(sale: RecentSale, delay: number) {
    setTimeout(() => {
      const toast = createToast(sale);
      container?.appendChild(toast);
      
      // Animate in
      requestAnimationFrame(() => {
        toast.classList.remove('translate-x-[-100%]', 'opacity-0');
        toast.classList.add('translate-x-0', 'opacity-100');
      });

      // Auto hide after 4s
      setTimeout(() => {
        toast.classList.remove('translate-x-0', 'opacity-100');
        toast.classList.add('translate-x-[-100%]', 'opacity-0');
        setTimeout(() => toast.remove(), 500);
      }, 4000);
    }, delay);
  }

  // Show sequence: 3s, 8s, 15s
  function startSequence() {
    if (!container) return;
    showToast(mockSales[0], 3000);
    showToast(mockSales[1], 8000);
    showToast(mockSales[2], 15000);
    
    // Loop every 30s
    setInterval(() => {
      showToast(mockSales[0], 0);
      showToast(mockSales[1], 5000);
      showToast(mockSales[2], 12000);
    }, 30000);
  }

  // Start on page load
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', startSequence);
  } else {
    startSequence();
  }
</script>

<style>
  @keyframes shrink {
    from { width: 100%; }
    to { width: 0%; }
  }
</style>
